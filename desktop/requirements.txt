# Windows 桌面客户端打包依赖 = 后端运行时依赖(去掉 pytest/httpx 等测试件)+ PyInstaller。
# 与 backend/requirements.txt 的运行时部分保持一致,升级后端依赖时记得同步这里。
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pymupdf>=1.24
python-pptx>=1.0
pydantic>=2.7

# 打包工具
pyinstaller>=6.10
